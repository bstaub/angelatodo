<%- include('header') -%>    
    
    <div class="box">
            <% for( let i = 0; i < newListItem.length; i++ ) { %>
                <div class="item">
                    <input type="checkbox">
                    <p><%= newListItem[i] %></p> 
                </div>
             <% } %>

             <form class="item" action="/" method="POST">
                <input type="text" name="newItem" placeholder="Neuer Eintrag" autocomplete="off">
                <button type="submit" name="list" value=<%= listTitle %></button>+</button>
            </form>

    </div>


<%- include('footer') -%> 